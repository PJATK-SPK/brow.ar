<?xml version="1.0" encoding="utf-8"?>
<!--
    Project: Mobile App for Beer rating and commenting (students project)

    Description: In this app you can search your favorite beverage
    find out what people think about it in the comments as well as by rating
    and also rate it yourself according to your taste buds

    Author: Pawel Badysiak
    Author: Sandro Sobczynski
    Author: Marcel Pankanin
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.browar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity android:name=".Start" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BrowseBeers" android:exported="true"/>
        <activity android:name=".Beer" android:exported="true"/>
        <activity android:name=".RateBeer" android:exported="true"/>
        <activity android:name=".AddBeerActivity" android:exported="true"/>
        <activity android:name=".AddCommentActivity" android:exported="true"/>

    </application>

</manifest>